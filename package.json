{
  "name": "part3",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "rm -rf dist && cd ../Phonebook-frontend && npm run build && cp -r dist ../Phonebook-backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git subtree push --prefix=part3/Phonebook-backend subrepo main",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^5.2.1",
    "morgan": "^1.10.1"
  }
}
